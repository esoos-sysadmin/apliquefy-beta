# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files (o .env.example é versionado de propósito)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build outputs
.next/
out/
build
dist
dist-electron
dist-react
apps/desktop/release
apps/desktop/build/robots-dist

# Python (apps/robots)
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# graphify — grafo é derivado do código, cada dev roda `graphify update .`
# (2MB de JSON conflitam em todo merge; para compartilhar, use `graphify merge-driver` + .gitattributes)
graphify-out/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem
ngrok.yml

# preview local do PDF do currículo (gerado por apps/web/scripts/preview-resume-pdf.ts)
.preview/
